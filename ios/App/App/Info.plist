<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleDisplayName</key>
        <string>CANA</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>UILaunchStoryboardName</key>
	<string>LaunchScreen</string>
	<key>UIMainStoryboardFile</key>
	<string>Main</string>
	<!-- Required device capabilities: arm64 only. The Capacitor template
	     shipped armv7 here (a 32-bit ARM architecture dropped after iOS 10),
	     which contradicted our IPHONEOS_DEPLOYMENT_TARGET = 16.0 and triggered
	     App Store validator warnings. -->
	<key>UIRequiredDeviceCapabilities</key>
	<array>
		<string>arm64</string>
	</array>
	<!-- Export-compliance declaration for App Store / TestFlight. We use only
	     standard authentication / data-protection cryptography (PBKDF2 +
	     AES-GCM via the system Web Crypto API in WKWebView), which is exempt
	     under U.S. EAR § 740.17(b)(1). Declaring this here once means Apple
	     never asks at upload time. If we ever add encryption beyond standard
	     authentication, flip this and complete the ERN. -->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
	<!-- App-Bound Domains: deliberately empty. Combined with Capacitor's
	     `limitsNavigationsToAppBoundDomains: true`, this locks the WKWebView
	     to its bundled local content. CANA's iOS build never navigates to
	     external domains (no Ollama, no GitHub API, no analytics) — this
	     enforces that at the WebKit layer. -->
	<key>WKAppBoundDomains</key>
	<array/>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UIViewControllerBasedStatusBarAppearance</key>
	<true/>
</dict>
</plist>
